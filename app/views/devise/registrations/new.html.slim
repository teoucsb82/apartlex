.row
  .col-sm-12
    h2.m-t-none.m-b Sign up
    = simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
      = f.error_notification
      .form-group
        = f.input :email, required: true, autofocus: true, input_html: { class: "input-lg"}
      .form-group
        = f.input :password, required: true, input_html: { class: "input-lg"}
      = f.simple_fields_for :account do |account_form|
        = account_form.input :name, input_html: { class: "input-lg" }
      .form-group
        = f.input :remember_me, as: :hidden, input_html: { checked: true }, hidden: true if devise_mapping.rememberable?
      div
        button.btn.btn-lg.btn-primary.btn-block type="submit" 
          strong Sign up
.row
  .col-sm-12
    p
      | Already a member? Click 
      = link_to "here", new_session_path(resource_name)
      |  to log in
